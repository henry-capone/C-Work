#pragma once
#include <iostream>
#include <string>

/*
class Item {
private:
	std::string name;
	std::string description;
public:
	Item() {};
	Item(const std::string& name, const std::string& desc);
	std::string GetName();
	std::string GetDescription();
	bool operator== (Item other) { return other.name == name; }
};
*/

